# Supply-chain hardening: refuse to install package versions younger than 3 days
# (defends against newly-published malicious packages that get yanked within hours).
# pnpm 10.16+ required. Value is in minutes: 4320 = 3 days.
minimumReleaseAge=4320
